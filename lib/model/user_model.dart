class UserModel{
  final userName;
  final firstName;
  final lastName;
  final dob;
  final points;
  final kilometers;
  final profile;

  UserModel(this.userName, this.firstName, this.lastName, this.dob, this.points, this.kilometers, this.profile);

}